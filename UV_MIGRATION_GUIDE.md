# 🚀 BioForge с UV - Руководство по миграции

## 📋 Что такое UV?

**UV** - это современный, сверхбыстрый менеджер пакетов Python от Astral (создатели Ruff).

### Преимущества UV над Poetry:

| Характеристика | UV | Poetry |
|---------------|-----|---------|
| **Скорость установки** | ⚡ 10-100x быстрее | 🐌 Медленно |
| **Использование памяти** | 📉 Минимальное | 📈 Высокое |
| **Размер бинарника** | 🪶 ~10MB | 🏋️ ~50MB + Python |
| **Совместимость с pip** | ✅ 100% | ⚠️ Частичная |
| **Написан на** | 🦀 Rust | 🐍 Python |
| **Кеширование** | ✅ Глобальное | ✅ Локальное |
| **requirements.txt** | ✅ Нативная поддержка | 🔄 Требует экспорт |

---

## 🛠️ Установка UV

### Windows
```powershell
# Способ 1: Через PowerShell (рекомендуется)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# Способ 2: Через pip
pip install uv

# Способ 3: Через pipx
pipx install uv
```

### macOS/Linux
```bash
# Способ 1: Через curl (рекомендуется)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Способ 2: Через pip
pip install uv

# Способ 3: Через Homebrew (macOS)
brew install uv
```

---

## 📦 Автоматическая миграция

### Шаг 1: Запустите скрипт миграции
```bash
python migrate_to_uv.py
```

Скрипт автоматически:
- ✅ Установит UV
- ✅ Создаст новый pyproject.toml
- ✅ Сгенерирует requirements.txt
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Создаст скрипты запуска

---

## 🎯 Основные команды UV

### Управление виртуальным окружением
```bash
# Создать новое окружение
uv venv

# Создать окружение с конкретной версией Python
uv venv --python 3.11

# Активировать окружение
# Windows:
.venv\Scripts\activate
# Unix:
source .venv/bin/activate
```

### Установка пакетов
```bash
# Установить пакет
uv pip install pandas

# Установить из requirements.txt
uv pip install -r requirements.txt

# Установить с дополнительными зависимостями
uv pip install "fastapi[all]"

# Установить конкретную версию
uv pip install "numpy==1.24.0"
```

### Управление зависимостями
```bash
# Показать установленные пакеты
uv pip list

# Заморозить зависимости
uv pip freeze > requirements.txt

# Обновить пакет
uv pip install --upgrade pandas

# Удалить пакет
uv pip uninstall pandas

# Синхронизировать с requirements.txt (удалит лишние пакеты)
uv pip sync requirements.txt
```

### Компиляция зависимостей
```bash
# Скомпилировать pyproject.toml в requirements.txt
uv pip compile pyproject.toml -o requirements.txt

# С обновлением до последних версий
uv pip compile --upgrade pyproject.toml -o requirements.txt

# Только для Python 3.8
uv pip compile --python-version 3.8 pyproject.toml -o requirements.txt
```

---

## 📁 Структура проекта с UV

```
BioForge_edit_branch/
├── .venv/                    # Виртуальное окружение UV
├── src/
│   └── genoscope/           # Исходный код
├── tests/                   # Тесты
├── pyproject.toml          # Конфигурация проекта для UV
├── requirements.txt        # Основные зависимости
├── requirements-dev.txt    # Dev зависимости
├── uv.lock                # Лок-файл UV (создается автоматически)
├── Makefile.uv           # Makefile для UV команд
├── start_with_uv.bat     # Скрипт запуска для Windows
└── start_with_uv.sh      # Скрипт запуска для Unix
```

---

## 🚀 Быстрый старт

### 1. Миграция за одну команду
```bash
# Запустить миграцию
python migrate_to_uv.py

# Активировать окружение (Windows)
.venv\Scripts\activate

# Запустить сервер
start_with_uv.bat
```

### 2. Использование Makefile
```bash
# Установить все зависимости (включая dev)
make -f Makefile.uv dev

# Запустить сервер
make -f Makefile.uv run

# Запустить тесты
make -f Makefile.uv test

# Форматировать код
make -f Makefile.uv format
```

---

## 🔄 Сравнение команд Poetry vs UV

| Действие | Poetry | UV |
|----------|---------|-----|
| Создать проект | `poetry new project` | `uv venv && uv pip install` |
| Установить зависимости | `poetry install` | `uv pip install -r requirements.txt` |
| Добавить пакет | `poetry add pandas` | `uv pip install pandas` |
| Добавить dev пакет | `poetry add --dev pytest` | `uv pip install pytest` |
| Обновить пакеты | `poetry update` | `uv pip install --upgrade -r requirements.txt` |
| Показать пакеты | `poetry show` | `uv pip list` |
| Запустить скрипт | `poetry run python script.py` | `python script.py` |
| Экспорт requirements | `poetry export -f requirements.txt` | `uv pip freeze > requirements.txt` |
| Виртуальное окружение | `poetry shell` | `source .venv/bin/activate` |

---

## ⚡ Производительность UV

### Тест скорости установки pandas + numpy + scikit-learn:

```bash
# Poetry: ~45 секунд
time poetry add pandas numpy scikit-learn

# pip: ~12 секунд
time pip install pandas numpy scikit-learn

# UV: ~0.8 секунд 🚀
time uv pip install pandas numpy scikit-learn
```

---

## 🔧 Продвинутые возможности UV

### 1. Глобальный кеш пакетов
UV автоматически кеширует загруженные пакеты глобально:
```bash
# Посмотреть размер кеша
uv cache dir

# Очистить кеш
uv cache clean
```

### 2. Установка из Git
```bash
# Из Git репозитория
uv pip install git+https://github.com/user/repo.git

# Конкретная ветка
uv pip install git+https://github.com/user/repo.git@branch

# Конкретный коммит
uv pip install git+https://github.com/user/repo.git@commit_hash
```

### 3. Установка локальных пакетов
```bash
# В режиме разработки (editable)
uv pip install -e .

# Обычная установка
uv pip install .
```

### 4. Работа с различными индексами
```bash
# Использовать альтернативный индекс
uv pip install --index-url https://test.pypi.org/simple/ package

# Добавить дополнительный индекс
uv pip install --extra-index-url https://my.index.com/simple/ package
```

---

## 📊 Мониторинг и отладка

### Verbose режим
```bash
# Подробный вывод
uv pip install pandas -v

# Очень подробный вывод
uv pip install pandas -vv
```

### Проверка совместимости
```bash
# Проверить конфликты зависимостей
uv pip check

# Показать дерево зависимостей
uv pip tree
```

---

## ❓ Частые вопросы

### Q: Можно ли использовать UV с существующим requirements.txt?
**A:** Да! UV полностью совместим с pip и requirements.txt:
```bash
uv pip install -r requirements.txt
```

### Q: Как перенести зависимости из Poetry?
**A:** Используйте скрипт миграции или:
```bash
poetry export -f requirements.txt -o requirements.txt --without-hashes
uv pip install -r requirements.txt
```

### Q: Поддерживает ли UV lock-файлы?
**A:** Да, UV автоматически создает uv.lock при установке пакетов.

### Q: Можно ли использовать UV в CI/CD?
**A:** Да! UV идеально подходит для CI/CD благодаря скорости:
```yaml
# GitHub Actions
- name: Install UV
  run: pip install uv

- name: Install dependencies
  run: uv pip install -r requirements.txt
```

---

## 🐛 Решение проблем

### Проблема: UV не найден после установки
```bash
# Windows: Добавить в PATH
$env:Path += ";$env:USERPROFILE\.cargo\bin"

# Unix: Добавить в .bashrc/.zshrc
export PATH="$HOME/.cargo/bin:$PATH"
```

### Проблема: Конфликты с Poetry
```bash
# Деактивировать Poetry окружение
deactivate

# Удалить Poetry окружение
rm -rf $(poetry env info --path)

# Создать новое UV окружение
uv venv
```

---

## 📚 Дополнительные ресурсы

- 🌐 [Официальный сайт UV](https://github.com/astral-sh/uv)
- 📖 [Документация UV](https://github.com/astral-sh/uv/blob/main/README.md)
- 💬 [Discord сообщество](https://discord.gg/astral)
- 🐛 [Issue tracker](https://github.com/astral-sh/uv/issues)

---

## ✅ Чеклист после миграции

- [ ] UV установлен и доступен в PATH
- [ ] Виртуальное окружение создано (.venv)
- [ ] Зависимости установлены
- [ ] Проект запускается без ошибок
- [ ] Тесты проходят успешно
- [ ] Poetry файлы сохранены в бэкапы
- [ ] Документация обновлена

---

## 🎉 Поздравляем!

Вы успешно мигрировали на UV! Теперь ваш проект:
- ⚡ Устанавливает зависимости в 10-100 раз быстрее
- 💾 Использует меньше памяти
- 🚀 Готов к современной разработке

**Начните работу:**
```bash
# Windows
start_with_uv.bat

# Unix
./start_with_uv.sh
```

---

*Документация создана для BioForge v1.0.0*  
*UV версия: последняя*
