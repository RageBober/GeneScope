# 🧬 GenoScope

*Инструмент для загрузки, очистки, фильтрации и базового анализа геномных данных (CSV / FASTA / VCF / BAM и др.).*

---

## 📐 Архитектура

1.GenoScope/
├── data_analysis/ # API-надстройка: ingest + cleaning + filters + visual
│ ├── data_ingestion.py # load_* функции
│ ├── data_cleaning.py # remove_duplicates, handle_missing, detect_outliers
│ ├── data_filtering.py # filter_outliers (+ вспомогательные)
│ ├── analysis_core.py # статистика, корреляция, PCA, отбор признаков
│ ├── visualization.py # гистограммы, heatmap, PCA-графики
│ ├── sequence_analysis.py # анализ биопоследовательностей
│ ├── data_filters/ # низкоуровневые фильтры
│ │ └── pipeline.py # run_filters orchestration
│ └── ...
├── MLmodel/ # AI-инструменты (импутация, baseline-модель)
│ └── data_cleaning_AI/ml_imputation.py
├── interface.py # Tkinter-GUI-прототип
├── main.py # CLI-точка входа
├── requirements.txt
└── tests/ # pytest-тесты

yaml
Копировать
Редактировать

---

## 🚀 Быстрый запуск

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python main.py --help
🏗️ Основные возможности
Загрузка данных из CSV, Excel, JSON, FASTA, VCF, BAM, HDF5, GFF.

Очистка: удаление дубликатов, обработка пропусков, обнаружение выбросов.

Гибкая фильтрация: value range, корреляция, кластеризация, pipeline.

Визуализация распределений, корреляций, PCA.

Статистический анализ, отбор признаков (feature selection).

ML-импутация (заполнение пропусков AI-моделью).

Расширяемый пайплайн фильтрации.

Прототип GUI (Tkinter), CLI-обёртка.

Покрытие юнит-тестами (pytest).

💡 Пример использования (CLI)
bash
Копировать
Редактировать
python main.py --input data/test.csv --out result.csv --dropna --remove-dupes --filter-outliers
🛠️ План развития
 ML-импутация в handle_missing_values(method='ml')

 CI + coverage ≥ 80 %

 Перенос GUI с Tkinter → Tauri

 Интеграция AlphaFold / XtrimoPGLM как внешних модулей

 docs/ и визуал архитектуры

🧑‍💻 Контакты и развитие
Telegram: @alfer_bio <!-- Поменяй на свой ник! -->

Email: yourmail@example.com <!-- Добавь почту, если надо -->

Вопросы/идеи: GitHub Issues