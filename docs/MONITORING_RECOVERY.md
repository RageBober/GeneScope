# 🔧 Восстановление мониторинга после очистки

## ⚠️ Проблема
При очистке проекта был ошибочно удален файл `grafana-enterprise_12.1.1_16903967602_linux_amd64.deb`

## ✅ Решения

### Вариант 1: Использовать Docker (Рекомендуется)
Ваша система уже настроена для Docker:

```bash
# Запуск только мониторинга
cd monitoring
docker-compose up -d

# Или запуск приложения с встроенным мониторингом  
docker-compose -f docker-compose.full.yml up -d
```

**Доступ к сервисам:**
- 📊 Grafana: http://localhost:3001 (admin/admin)
- 📈 Prometheus: http://localhost:9090
- 🗃️ База данных: localhost:5432
- 🔴 Redis: localhost:6379

### Вариант 2: Переустановить Grafana Enterprise
Если вам нужна именно Enterprise версия:

```bash
# Скачать актуальную версию
wget https://dl.grafana.com/enterprise/release/grafana-enterprise_10.2.0_amd64.deb

# Установить (если нужна системная установка)
sudo dpkg -i grafana-enterprise_10.2.0_amd64.deb
```

### Вариант 3: Использовать существующую конфигурацию
У вас есть готовая конфигурация в `monitoring/`:

```bash
# Быстрый запуск
./start_monitoring.sh

# Или вручную
cd monitoring && docker-compose up -d
```

## 🔍 Проверка работы

```bash
# Проверить состояние контейнеров
docker-compose ps

# Просмотреть логи
docker-compose logs grafana

# Доступ к Grafana
curl http://localhost:3001
```

## 📊 Настройка дашбордов

1. Откройте Grafana: http://localhost:3001
2. Войдите: admin/admin
3. Импортируйте дашборды из `monitoring/grafana/dashboards/`
4. Настройте источники данных:
   - Prometheus: http://prometheus:9090
   - Loki: http://loki:3100

## 🚀 Интеграция с приложением

Для полной интеграции используйте:
```bash
docker-compose -f docker-compose.full.yml up -d
```

Это запустит приложение + мониторинг в едином стеке.
