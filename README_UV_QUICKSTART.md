# 🚀 BioForge с UV - Быстрый старт

## 📦 Что нужно сделать

### Вариант 1: Полная установка (рекомендуется)
```bash
# Просто запустите этот файл:
complete_setup_uv.bat
```

Этот скрипт автоматически:
- ✅ Исправит все проблемы в коде
- ✅ Установит UV (супер-быстрый менеджер пакетов)
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Запустит сервер

---

### Вариант 2: Только UV setup
```bash
# Быстрая установка UV:
quick_uv_setup.bat
```

---

### Вариант 3: Полная миграция с Poetry
```bash
# Если у вас был Poetry:
python migrate_to_uv.py
```

---

## ⚡ Почему UV?

| | UV | pip | Poetry |
|--|-----|------|---------|
| **Скорость** | 0.8 сек | 12 сек | 45 сек |
| **Для установки pandas+numpy+sklearn** | ⚡⚡⚡ | 🐌 | 🐌🐌 |

---

## 🎯 После установки

### Запуск сервера:
```bash
# Активировать окружение
.venv\Scripts\activate

# Запустить API
uvicorn genoscope.api.main:app --reload
```

### Доступ к приложению:
- 📊 **API Docs:** http://localhost:8000/docs
- 🌐 **Web UI:** http://localhost:8000/ui

---

## 📋 Основные команды UV

```bash
# Установить пакет
uv pip install pandas

# Показать установленные пакеты
uv pip list

# Обновить пакет
uv pip install --upgrade pandas

# Установить из requirements.txt
uv pip install -r requirements.txt
```

---

## ❓ Проблемы?

### UV не установлен:
```bash
pip install uv
```

### Ошибки импорта:
```bash
python diagnostics\final_fix.py
```

### Зависимости не установлены:
```bash
uv pip install -r requirements.txt
```

---

## 📊 Структура проекта

```
BioForge/
├── .venv/               # Виртуальное окружение UV
├── src/genoscope/       # Код проекта
├── requirements.txt     # Зависимости
├── complete_setup_uv.bat # Полная установка
└── quick_uv_setup.bat   # Быстрая установка UV
```

---

**Готово к работе за 2 минуты с UV! 🚀**
